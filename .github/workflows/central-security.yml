name: Central Security Scan

on:
  pull_request:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    uses: kkz6/security-workflows/.github/workflows/semgrep.yml@main
